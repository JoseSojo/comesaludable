import { Apple, User2 } from "lucide-react"

export const HandlerIco = ({customClass,ico}:{customClass?:string,ico:string}) => {

    if(ico === `user`) return <User2 className={customClass} /> 
    else if(ico === `apple`) return <Apple className={customClass} /> 
    else if(ico === `user`) return <User2 className={customClass} /> 
    else if(ico === `user`) return <User2 className={customClass} /> 
    else if(ico === `user`) return <User2 className={customClass} /> 
    else if(ico === `user`) return <User2 className={customClass} /> 
    else if(ico === `user`) return <User2 className={customClass} /> 
    else if(ico === `user`) return <User2 className={customClass} /> 
    else if(ico === `user`) return <User2 className={customClass} /> 
    else if(ico === `user`) return <User2 className={customClass} /> 
    else if(ico === `user`) return <User2 className={customClass} /> 
    else <></>
}
